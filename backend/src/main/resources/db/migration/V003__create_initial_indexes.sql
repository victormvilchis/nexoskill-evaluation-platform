CREATE INDEX IX_APP_USER_STATUS
    ON APP_USER (STATUS);

CREATE INDEX IX_AUTH_SESSION_USER_STATUS
    ON AUTH_SESSION (USER_ID, STATUS);

CREATE INDEX IX_AUTH_SESSION_EXPIRATION
    ON AUTH_SESSION (EXPIRES_AT, STATUS);

CREATE INDEX IX_LOGIN_ATTEMPT_EMAIL_DATE
    ON LOGIN_ATTEMPT (ATTEMPTED_EMAIL, ATTEMPTED_AT);

CREATE INDEX IX_LOGIN_ATTEMPT_IP_DATE
    ON LOGIN_ATTEMPT (IP_ADDRESS, ATTEMPTED_AT);

CREATE INDEX IX_AUDIT_EVENT_USER_DATE
    ON AUDIT_EVENT (USER_ID, OCCURRED_AT);

CREATE INDEX IX_AUDIT_EVENT_TYPE_DATE
    ON AUDIT_EVENT (EVENT_TYPE, OCCURRED_AT);
