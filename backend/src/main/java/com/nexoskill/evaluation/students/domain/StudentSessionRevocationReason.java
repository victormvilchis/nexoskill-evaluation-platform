package com.nexoskill.evaluation.students.domain;

public enum StudentSessionRevocationReason {
	LOGOUT, EXPIRED, DEACTIVATED, SUSPENDED, ARCHIVED, DELETED, PASSWORD_CHANGED, TEMP_PASSWORD_EXPIRED, PASSWORD_RESET,
	ADMIN_REVOKED, ORGANIZATION_UNAVAILABLE
}
