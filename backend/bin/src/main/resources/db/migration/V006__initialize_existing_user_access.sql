INSERT INTO USER_ACCESS (
    USER_ID,
    STARTS_AT,
    EXPIRES_AT,
    STATUS,
    CREATED_AT
)
SELECT
    u.USER_ID,
    SYSTIMESTAMP,
    NULL,
    'ACTIVE',
    SYSTIMESTAMP
FROM APP_USER u
WHERE NOT EXISTS (
    SELECT 1
    FROM USER_ACCESS a
    WHERE a.USER_ID = u.USER_ID
);
