export interface DevelopmentOption { value: string; label: string; count: number }
export interface DevelopmentProfile { displayName: string; firstName: string; professionalProfile: string | null; technologicalProfile: string | null; technology: string | null; expertiseLevel: string | null }
export interface HealthCheck { level: 'ALL_GOOD' | 'CONTINUE' | 'REQUIRES_ATTENTION' | 'ATTENTION_PRIORITY'; label: string; description: string }
export interface ActionLink { action: string; label: string; target: string }
export interface CurrentFocus { type: string; severity: string; title: string; description: string; actions: ActionLink[] }
export interface ContinueItem { type: string; title: string; subtitle: string; current: number | null; total: number | null; progressPercent: number | null; target: string }
export interface CertificationCard { type: string; label: string; status: 'IN_RULE' | 'EXPIRING_SOON' | 'EXPIRED' | 'PENDING_DEACTIVATION'; statusLabel: string; deadlineDate: string | null; applicationDate: string | null; expirationDate: string | null; score: number | null; attemptNumber: number | null; secondAttemptFailed: boolean; technology: string | null; level: string | null }
export interface EvaluationCard { assignmentPublicId: string; formPublicId: string; title: string; description: string | null; status: string; questionCount: number; durationMinutes: number | null; passingScore: number; maxAttempts: number | null; attemptsUsed: number; dueAt: string | null; canStart: boolean; canResume: boolean; activeAttemptPublicId: string | null }
export interface PreparationPoint { key: string; label: string; answered: number; correct: number; percentage: number | null }
export interface TrendPoint { period: string; answered: number; correct: number; percentage: number | null }
export interface Recommendation { type: string; severity: string; title: string; description: string; actionLabel: string; target: string }
export interface ActivityItem { type: string; occurredAt: string; title: string; description: string }
export interface HomeView { profile: DevelopmentProfile; healthCheck: HealthCheck; focus: CurrentFocus; continueItem: ContinueItem | null; certificationsEnabled: boolean; availableStudyQuestions: number; certifications: CertificationCard[]; evaluations: EvaluationCard[]; paths: never[]; preparation: PreparationPoint[]; strengths: PreparationPoint[]; improvementAreas: PreparationPoint[]; recommendations: Recommendation[]; trend: TrendPoint[]; recentActivity: ActivityItem[] }
export interface StudyOptions { technologies: DevelopmentOption[]; categories: DevelopmentOption[]; availableQuestions: number; reviewErrorQuestions: number }
export interface PracticeQuestionOption { publicId: string; text: string }
export interface PracticeQuestion { sessionQuestionPublicId: string; questionPublicId: string; position: number; total: number; type: string; statement: string; codeLanguage: string | null; codeContent: string | null; technology: string | null; categories: string[]; options: PracticeQuestionOption[]; answered: boolean; correct: boolean | null; selectedOptionPublicIds: string[]; correctOptionPublicIds: string[]; explanation: string | null }
export interface PracticeSession { publicId: string; mode: string; status: string; technology: string | null; category: string | null; requestedCount: number; totalQuestions: number; answeredQuestions: number; correctAnswers: number; startedAt: string; completedAt: string | null; immediateFeedback: boolean; currentQuestion: PracticeQuestion | null; questions: PracticeQuestion[] }
export interface PracticeFeedback { correct: boolean | null; correctOptionPublicIds: string[]; explanation: string | null; answeredQuestions: number; correctAnswers: number; completed: boolean; nextQuestion: PracticeQuestion | null }
export interface PracticeResult { publicId: string; mode: string; totalQuestions: number; answeredQuestions: number; correctAnswers: number; percentage: number; byTopic: PreparationPoint[]; incorrectQuestionPublicIds: string[]; completedAt: string }
export interface EvaluationQuestion { attemptQuestionPublicId: string; questionPublicId: string; position: number; total: number; type: string; statement: string; codeLanguage: string | null; codeContent: string | null; options: PracticeQuestionOption[]; matchingRightOptions: PracticeQuestionOption[]; points: number; answered: boolean; selectedOptionPublicIds: string[]; matchingPairs: Record<string,string>; textAnswer: string | null }
export interface EvaluationAttempt { publicId: string; assignmentPublicId: string; formPublicId: string; title: string; status: string; attemptNumber: number; maxAttempts: number | null; durationMinutes: number | null; passingScore: number; startedAt: string; expiresAt: string | null; submittedAt: string | null; score: number | null; passed: boolean | null; showProgress: boolean; showQuestionNumbers: boolean; allowSaveResume: boolean; currentQuestion: EvaluationQuestion | null; questions: EvaluationQuestion[] }
export interface EvaluationAnswerReview { attemptQuestionPublicId: string; correct: boolean | null; correctOptionPublicIds: string[]; correctMatchingPairs: Record<string,string>; acceptedTextAnswers: string[] }
export interface EvaluationSubmission { attemptPublicId: string; status: string; score: number | null; passed: boolean | null; answeredQuestions: number; totalQuestions: number; message: string; answerReview: EvaluationAnswerReview[] }
export interface OrganizationBrand { global: boolean; organizationPublicId: string | null; name: string; hasLogo: boolean; logoUrl: string | null }

export interface StudentExperienceConfig { branding: OrganizationBrand; certificationsEnabled: boolean }
