import { useNavigate } from 'react-router-dom';import { createQuestion } from '../features/questions/api/questionApi';import { QuestionEditor } from '../features/questions/components/QuestionEditor';import { useToast } from '../shared/components/ToastProvider';import type { QuestionPayload } from '../shared/types/questions';
export function CreateQuestionPage(){const navigate=useNavigate(),toast=useToast();async function save(p:QuestionPayload){const q=await createQuestion(p);toast.success('Pregunta creada');navigate(`/admin/questions/${q.publicId}`)}return <main className="content-page editor-page"><div className="page-heading compact"><div><p className="eyebrow">Banco de preguntas</p><h1>Nueva pregunta</h1><p className="muted">Configura contenido, categorías y tipo de respuesta.</p></div></div><QuestionEditor onSubmit={save} submitLabel="Crear pregunta"/></main>}
